import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.votorantim.comissionamento',
  appName: 'Comissionamento-Off',
  webDir: 'dist'
};

export default config;
